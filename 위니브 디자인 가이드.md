# 위니브(weniv.co.kr) 디자인 가이드

> 출처: https://weniv.co.kr 실제 렌더링된 CSS 변수 · 컴퓨티드 스타일 추출
> 추출일: 2026-08-27
> 용도: 위니브 교육용 발표자료(HTML) 제작 시 톤 일치

---

## 1. 타이포그래피

| 항목 | 값 |
|---|---|
| 본문 서체 | **Pretendard** (variable/static, 로컬 번들) |
| 보조 서체 | Noto Sans KR |
| 폴백 스택 | `Pretendard, -apple-system, "Apple SD Gothic Neo", "Malgun Gothic", "Noto Sans KR", sans-serif` |
| 본문 색 | `#121314` (grayLv1) |
| 본문 크기 | 16px 기준 |

### 실측 위계

| 요소 | 크기 | 굵기 |
|---|---|---|
| H1 (로고/최상단) | 32px | 700 |
| H2 (섹션 타이틀) | 24 ~ 25.6px | 600 ~ 700 |
| H3 (카드 제목) | 18.72px | 700 |
| 본문 | 16px | 400 |
| 캡션 / 푸터 | 12px | 400 ~ 700 |

- 제목은 **700**, 소제목은 600, 본문은 400. 중간 굵기(500)를 잘 쓰지 않습니다.
- 한글 가독성을 위해 자간은 기본값 또는 아주 살짝 좁게(-0.01em) 씁니다.

---

## 2. 컬러 팔레트 (CSS 변수 원본)

### 브랜드 / 포인트

| 토큰 | 값 | 용도 |
|---|---|---|
| `--primary` | `#3075FF` | 브랜드 블루. 포인트 컬러 |
| `--primary-lighten` | `#2E6FF2` | 링크·활성 텍스트에 실제로 가장 많이 쓰임 |
| `--primary-darken` | `#145DF0` | hover / pressed |
| `--primary-a11y` | `#2861D2` | 흰 배경 위 텍스트용 (대비 확보) |
| `--primary-text` | `#FFFFFF` | 블루 위에 얹는 글자 |

### 그레이 스케일 (위니브의 실질적 뼈대)

| 토큰 | 값 | 용도 |
|---|---|---|
| `--grayLv1` | `#121314` | 제목·본문 텍스트, 다크 섹션 배경 |
| `--grayLv2` | `#595F66` | 보조 텍스트 |
| `--grayLv3` | `#8D9299` | 비활성 텍스트, 캡션 |
| `--grayLv4` | `#F3F5FA` | **카드 배경 / 보더 — 가장 자주 등장** |
| `--background` | `#1F2123` | 다크 배경 |
| `--activation` | `#2B3444` | 짙은 남색 계열 강조면 |

### 서피스

| 토큰 | 값 |
|---|---|
| `--surface` | `#FFFFFF` |
| `--surface-lighten` | `#F5F8FF` (아주 옅은 블루 틴트) |
| `--surface-darken` | `#E5EDFF` |

### 상태 / 코드 하이라이트

| 토큰 | 값 |
|---|---|
| `--error` | `#FC7377` |
| `--warn` | `#FFE187` |
| `--code-pink` | `#ED4779` |
| `--code-purple` | `#C893FD` |
| `--code-blue` | `#618DFF` |
| `--code-green` | `#50C140` |
| `--code-orange` | `#FFA52A` |

> **핵심**: 위니브는 화면의 90%를 무채색(흰색 + `#F3F5FA` + `#121314`)으로 쓰고,
> 블루(`#3075FF`)는 **딱 필요한 곳에만** 찍습니다. 블루 면적이 넓어지면 위니브 톤이 아니게 됩니다.

---

## 3. 형태 (Shape)

| 항목 | 값 |
|---|---|
| 라운드 | `10px`(작은 버튼) · `12px` · `16px` · `18px` · `20px`(카드) · `50%`(아바타) |
| 보더 | `2px solid #F3F5FA` — 카드 테두리의 기본형 |
| 그림자 | 거의 쓰지 않음. 필요 시 `0 8px 12px hsla(0,0%,7%,.2)` |
| 그라디언트 | 이미지 위 가독성용 오버레이만 (`rgba(18,19,20,.75)`) |

- **그림자 대신 옅은 회색 보더**로 면을 나눕니다. 이게 위니브 카드의 특징입니다.
- 카드 라운드는 20px 전후로 넉넉하게.

---

## 4. 레이아웃 / 컴포넌트 패턴

- **카드**: 흰 배경 + `2px solid #F3F5FA` + `border-radius: 20px` + 넉넉한 내부 여백(24~32px)
- **섹션 배경**: 흰색과 `#F3F5FA`를 번갈아 써서 구획을 나눔
- **히어로 배너**: 이미지 위 `rgba(18,19,20,.75)` 오버레이 + 흰 텍스트, 내부 여백 `60px 60px 50px`
- **활성 상태 표시**: 색을 칠하지 않고 **텍스트 색을 `#2E6FF2` + 굵기 700**으로 바꿔 표현 (페이지네이션 실측)
- **버튼**: 채도 낮게. 보조 버튼은 `#F3F5FA` 배경 + `#121314` 글자 + 라운드 10px

---

## 5. 발표자료에 옮길 때의 규칙 (요약)

1. 배경은 흰색, 면 구분은 `#F3F5FA`. 다크 슬라이드는 `#121314`.
2. 제목 700 / 본문 400, 서체는 Pretendard 하나로 통일.
3. 블루는 **강조 1~2곳**만. 숫자·키워드·밑줄 정도.
4. 카드에 그림자를 넣지 말고 `2px #F3F5FA` 보더로.
5. 라운드는 20px, 작은 칩은 10px.
6. 코드/JSON 블록은 다크(`#1F2123`) 위에 `--code-*` 팔레트로.
