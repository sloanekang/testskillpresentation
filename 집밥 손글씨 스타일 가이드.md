# 집밥 손글씨 감성 이미지 — 스타일 가이드

> 참고 원본: `집밥 손글씨1.jpg`, `집밥 손글씨2.jpg`
> 용도: 집밥 사진 → 손글씨 라벨을 얹은 인스타그램 게시용 이미지
> 작성일: 2026-08-25

---

## 0. 먼저, Skill로 만들 만한 일인가?

**결론: 만들 가치가 있습니다.** 다만 "스크립트 하나"가 아니라 **판단 + 렌더링이 결합된 Skill**이어야 합니다.

| 판단 기준 | 이 작업의 경우 | 평가 |
|---|---|---|
| 반복성 | 집밥 사진 찍을 때마다 = 주 2~4회 | ✅ 높음 |
| 결과물 규격이 고정적인가 | 색·폰트·데코·로고 블록이 매번 동일 | ✅ 고정 |
| 매번 달라지는 부분이 있는가 | 음식 종류, 접시 위치, 문구 | ⚠️ 있음 |
| 사람의 감각이 꼭 필요한가 | 문구 톤은 규칙화 가능, 최종 선택만 사람 | ✅ 위임 가능 |

핵심은 **"매번 달라지는 부분"**입니다. 접시가 어디 있고 무슨 음식인지는 사진마다 다르므로 순수 자동화 스크립트로는 안 되고, 이미지를 보고 판단하는 AI가 필요합니다. 반대로 폰트·색·데코 규칙은 매번 똑같으니 AI가 매번 새로 정할 필요가 없습니다.

**→ 그래서 Skill이 정답입니다.** Skill = 고정 규칙(이 문서) + AI 판단(무엇을 어디에) + 실행 도구(렌더링).

### 권장 Skill 구조

```
homework-집밥손글씨/
├── SKILL.md                  # 실행 절차 (아래 §6 워크플로우)
├── references/
│   ├── style-guide.md        # 이 문서
│   └── copy-bank.md          # 문구 사전 (§5)
├── assets/
│   └── fonts/                # 한글 손글씨 TTF
└── scripts/
    └── render_overlay.py     # JSON 스펙 → 이미지 합성
```

---

## 1. 사진 자체의 조건

이 스타일은 **사진이 절반**입니다. 손글씨를 얹기 좋은 사진의 조건:

- **앵글**: 정확히 위에서 내려다본 탑다운(90°). 비스듬하면 여백이 사다리꼴로 일그러져 글씨 놓을 자리가 사라집니다.
- **배경**: 흰색 또는 밝은 회색 식탁(대리석 무늬 상판이 이상적). 흰 글씨의 대비를 만드는 유일한 장치입니다.
- **접시**: 흰 접시 위주. 접시 사이를 **주먹 하나 정도씩 벌려서** 배치 — 이 틈이 글씨 자리입니다.
- **조명**: 밝은 자연광 또는 확산광. 강한 그림자는 글씨를 지저분하게 만듭니다.
- **보정**: 밝기 +, 채도 살짝 −, 화이트 밸런스는 차갑게. 전체적으로 뽀얀 화이트 톤.
- **비율**: 인스타 캐러셀은 **4:5 세로(1080×1350)** 권장. 여러 장이면 전 장 동일 비율.

> ⚠️ 사진 가장자리를 접시로 꽉 채우면 이 스타일이 성립하지 않습니다. **여백이 곧 캔버스**입니다.

---

## 2. 손글씨 텍스트

### 색상

| 요소 | 값 | 비고 |
|---|---|---|
| 기본 텍스트 | `#FFFFFF` ~ `#F2FAFF` | 원본 측정값 평균 `#F0FBFE` (아주 옅은 쿨톤 화이트) |
| 그림자 | `#000000`, 투명도 15~25%, 블러 3~4px, 오프셋 (2, 2) | 밝은 배경 위 가독성 확보용. 항상 넣되 티 나지 않게 |
| 예외 | 없음 | 컬러 텍스트를 섞으면 즉시 이 스타일이 아니게 됩니다 |

### 폰트

얇고 둥근 펜글씨체. 붓글씨·캘리그라피처럼 굵고 흘려 쓴 것은 톤이 다릅니다.

추천 후보 (모두 무료 배포, 다운로드 페이지에서 상업적 이용 조건 한 번 확인 권장):

- **나눔손글씨 시리즈** (네이버 클로바) — `다시 시작해`, `고운밤`, `규리의 일기`, `세아체`
- **카페24 써라운드** — 둥글고 정돈된 느낌
- **배달의민족 주아체** — 더 통통한 느낌, 캐주얼하게 가고 싶을 때

> 세 개쯤 다운받아 같은 문구로 렌더해보고 하나를 **고정**하세요. 매번 폰트가 바뀌면 계정 톤이 무너집니다.

### 크기 (이미지 세로 길이 기준 비율)

| 종류 | 글자 높이 | 1080×1350 환산 |
|---|---|---|
| 일반 라벨 | 3.0 ~ 3.5% | 40 ~ 47px |
| 강조 문구 (오늘의 메인 등) | 3.5 ~ 4.0% | 47 ~ 54px |
| 시그니처 "집밥 기록" | 4.0 ~ 4.5% | 54 ~ 61px |

### 조판

- **줄바꿈**: 5자 넘으면 2줄. `살얼음 메밀 / 시원해!`처럼 의미 단위로 끊습니다.
- **행간**: 글자 높이의 1.4~1.5배 (넉넉하게)
- **자간**: 기본 + 2~4% (살짝 벌려야 손글씨 느낌이 삽니다)
- **회전**: −3° ~ +3° 범위에서 라벨마다 랜덤. 전부 수평이면 인쇄물처럼 보입니다.
- **정렬**: 화살표가 나가는 쪽 기준 정렬 (화살표가 오른쪽 아래로 향하면 텍스트는 왼쪽 정렬)

---

## 3. 화살표

- **형태**: 얇은 흰색 **곡선**. 직선 화살표는 쓰지 않습니다.
- **두께**: 2~3px (1080px 기준). 텍스트 획보다 가늘게.
- **길이**: 이미지 너비의 5~12%. 짧을수록 자연스럽습니다.
- **곡률**: 완만한 C자. 시작점과 끝점을 잇는 직선에서 길이의 15~25%만큼 부풀립니다.
- **화살촉**: 끝에 작은 V자 두 획. 삼각형으로 채우지 않습니다. 길이는 화살표 전체의 15% 내외.
- **손떨림**: 경로에 ±1~2px 미세 노이즈를 주면 훨씬 손그림 같아집니다.
- **방향**: 텍스트 → 음식. 라벨 위치에 따라 ↙ ↘ ↖ ↗ 자유롭게 섞습니다.
- **개수**: 라벨 6~9개 중 **4~6개에만** 화살표를 붙입니다. 전부 붙이면 답답합니다.

---

## 4. 장식(데코)과 시그니처

### 데코 요소

| 요소 | 개수 | 크기 | 배치 |
|---|---|---|---|
| ✦ 4각 반짝이별 | 3 ~ 6개 | 글자 높이의 40~90% (크기를 섞을 것) | 여백, 텍스트 옆 |
| ♡ 작은 하트 | 2 ~ 4개 | 글자 높이의 50~70% | 라벨 끝, 코너 여백 |
| 짧은 사선 3개 묶음 / 작은 점 | 2 ~ 3세트 | 아주 작게 | 별 주변, 빈 구석 |

- **총 8~15개**를 넘기지 마세요. 이 스타일의 절제미는 데코 개수에서 나옵니다.
- **음식 위에는 데코를 놓지 않습니다.** 여백 전용입니다.
- 별은 정중앙에 몰리지 않게 프레임 전체에 흩뿌립니다.

### 시그니처 블록 — "집밥 기록"

계정 정체성을 만드는 요소이므로 **매번 같은 자리·같은 형태**로 넣습니다.

- **위치**: 하단 좌·우 코너 중 여백이 더 많은 쪽
- **변형 A** (참고1): 텍스트 + 아래 손그림 밑줄 + 옆에 작은 별
- **변형 B** (참고2): 텍스트를 감싸는 러프한 손그림 사각 프레임. 모서리는 딱 안 맞게, 오른쪽 아래에 두 번째 선을 살짝 겹쳐 두께감

> 둘 중 하나를 고정해도 좋고, 캐러셀 1장은 A · 마지막 장은 B처럼 규칙을 만들어도 좋습니다.

---

## 5. 문구 사전 (copy bank)

이 스타일의 진짜 핵심입니다. **음식 이름을 그대로 쓰지 않습니다.** 감각어를 붙이거나, 역할을 지정하거나, 혼잣말을 적습니다.

### 5-1. 유형별

| 유형 | 예시 | 쓰는 자리 |
|---|---|---|
| **감각어 + 음식명** | 살얼음 메밀 / 감칠맛 메밀 / 새콤달콤 비빔국수 / 잔치온국수 | 메인 요리 |
| **의성어·의태어 단독** | 아삭아삭 / 바삭바삭 / 쫄깃쫄깃 / 보들보들 / 사르르 | 식감이 포인트인 반찬 |
| **역할 라벨** | 오늘의 메인 / 면 추가 / 계란 추가 / 반찬 조합 / 소스 / 물 한 잔 | 조연 그릇, 부재료 |
| **혼잣말·감탄** | 수박 달다 ♡ / 시원해! / 이건 못 참지 / 한 그릇 뚝딱 | 후식, 강조하고 싶은 하나 |

### 5-2. 작성 규칙

- 길이: **2~7자**, 최대 10자. 문장이 아니라 **메모**입니다.
- 개수: 사진 한 장당 **6~9개**
- **마침표 금지**. 느낌표·하트는 전체에서 **1~2개만**.
- 같은 유형을 3개 이상 연달아 쓰지 않습니다 (감각어만 6개 → 지루함)
- 반드시 **역할 라벨을 2개 이상** 섞습니다. "면 추가", "물 한 잔" 같은 사소한 라벨이 생활감을 만듭니다.
- 캐러셀 마지막 장에는 **"집밥 기록"**을 크게 배치해 시리즈를 닫습니다.

---

## 6. 배치 규칙 (레이아웃)

1. **음식 위에 글씨를 얹지 않습니다.** 라벨은 전부 접시 사이·바깥 여백에.
2. 가장자리 안전 여백: 이미지 너비의 **3% 이상** 띄웁니다.
3. **인스타 UI 회피 구역** — 캐러셀에서 가려지는 자리입니다.
   - 좌하단: 프로필 아이콘
   - 우하단: 음소거 버튼
   - 우상단: 페이지 인디케이터(1/2)
   → 각 코너에서 약 12% 반경은 비워둡니다.
4. 라벨을 프레임 **상·하·좌·우에 고르게** 분산합니다. 한쪽에 몰리면 무게중심이 기웁니다.
5. 라벨끼리 최소 간격: 글자 높이의 1.5배 이상.

---

## 7. 구현 방식 A — 코드로 직접 합성 (권장)

결과가 매번 일정하고, 좌표를 수정해 재렌더할 수 있습니다.

### 아키텍처: "AI가 좌표를 짜고, 스크립트가 그린다"

```
사진 입력
  → [AI] 이미지를 보고 음식 식별 + 여백 탐색 + 문구 선정
  → [AI] 배치 스펙(JSON) 생성
  → [스크립트] Pillow로 텍스트·화살표·데코 렌더
  → 미리보기 확인 → 좌표만 수정해 재렌더
```

AI에게 그림을 그리게 하지 않고 **좌표만 정하게 하는 것**이 이 구조의 핵심입니다. 스타일은 스크립트가 보장하고, 판단만 AI가 합니다.

### 배치 스펙 JSON 예시

```json
{
  "image": "집밥_20260825.jpg",
  "canvas": { "width": 1080, "height": 1350 },
  "labels": [
    {
      "text": "살얼음 메밀\n시원해!",
      "x": 0.28, "y": 0.06,
      "size": 0.033,
      "rotate": -2,
      "align": "left",
      "arrow": { "to_x": 0.42, "to_y": 0.17, "curve": 0.2 }
    },
    {
      "text": "계란 추가",
      "x": 0.74, "y": 0.30,
      "size": 0.030,
      "rotate": 1,
      "arrow": { "to_x": 0.63, "to_y": 0.36, "curve": -0.18 }
    },
    { "text": "면 추가", "x": 0.17, "y": 0.72, "size": 0.030, "rotate": 2, "arrow": null }
  ],
  "decorations": [
    { "type": "sparkle", "x": 0.24, "y": 0.36, "scale": 0.9 },
    { "type": "sparkle", "x": 0.86, "y": 0.14, "scale": 0.6 },
    { "type": "heart",   "x": 0.93, "y": 0.55, "scale": 0.7 }
  ],
  "signature": { "text": "집밥 기록", "position": "bottom-right", "style": "underline" }
}
```

> 좌표는 모두 **0~1 비율**로 씁니다. 사진 해상도가 바뀌어도 그대로 재사용됩니다.

### 렌더링 스크립트 요구사항

- 라이브러리: `Pillow` (텍스트·도형), 필요시 `numpy` (곡선 계산)
- 텍스트: `ImageFont.truetype()`으로 손글씨 TTF 로드 → 그림자 레이어 먼저 그리고 그 위에 흰 텍스트
- 회전: 텍스트를 투명 레이어에 그린 뒤 `rotate(expand=True)` 후 합성
- 화살표: 2차 베지어 곡선을 40~60개 점으로 샘플링 → `line()` 연결, 각 점에 ±1.5px 노이즈
- 화살촉: 곡선 끝 접선 방향 기준 ±25° 두 획
- 별(sparkle): 중심에서 상하좌우로 뻗는 오목한 4각 폴리곤
- 출력: JPEG 품질 95, 원본 EXIF 방향 반영

### 사전 준비 체크

- [ ] 손글씨 TTF 파일 확보 후 `assets/fonts/`에 배치
- [ ] 폰트가 한글 완성형을 모두 지원하는지 확인 (일부 손글씨 폰트는 글자 누락)
- [ ] `pip install pillow`

---

## 8. 구현 방식 B — AI 이미지 생성 프롬프트

손맛은 더 자연스럽지만 **한글이 깨질 수 있고 결과가 매번 달라집니다.** 스타일 탐색·시안용으로 쓰고, 최종본은 방식 A를 권합니다.

### 프롬프트 템플릿

```
[이미지 편집 모드 / image-to-image로 실행]

원본 사진의 음식·접시·테이블은 절대 수정하지 마세요. 픽셀 그대로 보존하고,
그 위에 손글씨 오버레이 레이어만 추가합니다.

스타일:
- 순수한 흰색(#FFFFFF)의 얇고 둥근 한글 손글씨. 아주 옅은 그림자로 가독성 확보
- 각 라벨 옆에 손으로 그린 듯한 얇은 흰색 곡선 화살표(작은 V자 화살촉)가 해당 음식을 가리킴
- 여백에 작은 4각 반짝이별과 하트를 총 8~12개 흩뿌림 (음식 위에는 금지)
- 전체적으로 밝고 뽀얀 한국식 "집밥 기록" 인스타 감성

배치:
- 모든 텍스트는 접시 사이 빈 공간에만. 음식 위에 절대 겹치지 않게
- 사진 가장자리에서 최소 3% 여백 유지
- 좌하단·우하단·우상단 코너는 비워둘 것

넣을 문구 (정확히 이대로, 오탈자 없이):
1. "살얼음 메밀 시원해!" → 왼쪽 위 국수 그릇
2. "감칠맛 메밀"       → 오른쪽 국수 그릇
3. "아삭아삭"          → 가운데 콩나물 접시
4. "계란 추가"         → 계란말이 접시
5. "면 추가"           → 왼쪽 냄비
6. "오늘의 메인"       → 아래쪽 덮밥
7. "집밥 기록"         → 오른쪽 아래 코너, 다른 글씨보다 크게, 아래 손그림 밑줄
```

### 주의사항

- **텍스트-투-이미지가 아니라 이미지 편집(inpainting/image-to-image)으로** 실행해야 원본 음식이 유지됩니다.
- 한글이 깨지면: 문구 개수를 줄이고(4~5개) 여러 번 나눠 생성하거나, 텍스트 없이 데코만 생성 후 텍스트는 방식 A로 얹습니다.
- 생성 후 **오탈자를 반드시 눈으로 확인**하세요. AI 이미지의 한글은 자주 틀립니다.

---

## 9. 워크플로우 (Skill 실행 절차)

1. **입력 확인** — 사진 파일 경로, 오늘 만든 음식 목록(사용자가 알려주면 정확도↑)
2. **사진 점검** — 탑다운인지, 여백이 있는지 확인. 부족하면 재촬영 제안
3. **음식 식별** — 각 접시가 무엇인지, 이미지 내 위치(비율 좌표) 파악
4. **문구 선정** — §5 문구 사전에서 유형을 섞어 6~9개 선정
5. **여백 탐색** — 각 음식 근처의 빈 공간을 찾아 라벨 좌표 결정 (§6 규칙 준수)
6. **JSON 스펙 생성** — §7 포맷대로 작성
7. **렌더링** — 스크립트 실행
8. **자가 검수** — §10 체크리스트로 확인, 문제 있으면 좌표만 고쳐 재렌더
9. **전달** — 결과 이미지 저장 + 미리보기

---

## 10. 최종 검수 체크리스트

- [ ] **원본 사진에 작업했는가** (이미 손글씨가 들어간 이미지에 덧씌우지 않았는가)
- [ ] 텍스트가 음식이나 다른 텍스트를 가리지 않는가
- [ ] 텍스트가 사진 밖으로 잘리지 않았는가
- [ ] 화살표가 엉뚱한 음식을 가리키지 않는가
- [ ] 한글 오탈자가 없는가
- [ ] 데코가 15개를 넘지 않는가, 음식 위에 있지 않은가
- [ ] "집밥 기록" 시그니처가 들어갔는가
- [ ] 인스타 UI 회피 구역(코너 3곳)이 비어 있는가
- [ ] 라벨이 한쪽에 몰려 있지 않은가
- [ ] 느낌표·하트가 2개 이하인가

> 📌 `test_렌더링_결과.jpg`에서 글씨가 겹쳐 보인 원인은 **이미 손글씨가 들어간 참고 이미지를 원본으로 사용**했기 때문입니다. 반드시 아무것도 없는 원본 사진에 적용하세요.

---

## 11. 다음 단계

이 문서가 준비되었으니, 다음 프롬프트로 Skill 제작을 이어가면 됩니다.

```
집밥 손글씨 스타일 가이드.md를 이 폴더에 저장해뒀어.
이 가이드대로 집밥 사진에 손글씨를 얹어주는 스킬을 만들어줘.
- 이름 앞에 homework를 붙여줘
- 렌더링은 Python(Pillow) 방식으로, 배치 스펙 JSON을 거치는 구조로
- 문구 사전은 references로 분리해줘
- 손글씨 폰트 후보 3개를 같은 문구로 렌더한 비교 이미지를 먼저 보여줘
```
